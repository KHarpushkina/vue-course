"use strict";

export default {
    getArticles(context) {
        return context.articles;
    },

    /* getDimensions(context) {
        return (source) => {
            return context[source].dimensions;
        };
    } */
};